import { Pool } from 'pg';

/**
 * OrgRepository — stub.
 */
export class OrgRepository {
  constructor(private readonly _pool: Pool) {}

  // TODO: create, findById, findBySlug, addMember, removeMember, listMembers
}
