import { Pool } from 'pg';

/**
 * QueueRepository — stub.
 */
export class QueueRepository {
  constructor(private readonly _pool: Pool) {}

  // TODO: findById, findByProjectId, create, update, pause, resume, delete
}
